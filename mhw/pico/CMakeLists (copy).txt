cmake_minimum_required(VERSION 3.12)

add_library(hw_pico_modules 
    pwm_irq_handler.c
    temperatureHw.c
    ledHw.c
    onewirehl.c
    wiznet_init.c
    )

# generate the header file into the source tree as it is included in the RP2040 datasheet
pico_generate_pio_header(hw_pico_modules ${CMAKE_CURRENT_LIST_DIR}/ws2812.pio OUTPUT_DIR ${CMAKE_CURRENT_LIST_DIR}/generated)

target_link_libraries(hw_pico_modules 
pico_stdlib hardware_pwm hardware_spi pico_unique_id hardware_pio 
drivers rp
wiznet)

#target_include_directories(hw_pico_modules PUBLIC "${PROJECT_SOURCE_DIR}")
#target_include_directories(hw_pico_modules PUBLIC "${PROJECT_BINARY_DIR}/tif/tif")
target_include_directories(hw_pico_modules PRIVATE "${TIF_PATH}")